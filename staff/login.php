<?php require_once __DIR__.'/../includes/layout.php';if(staff()){header('Location: /staff/index.php');exit;}$err='';if($_SERVER['REQUEST_METHOD']==='POST'){check_csrf();$q=db()->prepare('SELECT * FROM staff WHERE username=? AND active=1');$q->execute([trim($_POST['username'])]);$s=$q->fetch();if($s&&password_verify($_POST['password'],$s['password_hash'])){session_regenerate_id(true);$_SESSION['staff']=$s;audit('staff_login','staff',$s['id'],$s['center_id']);header('Location: /staff/index.php');exit;}$err='Invalid username or password.';}head('Staff Login');?><div class="login card"><a class="brand" href="/index.php">Thal<span>Vital</span></a><h2>Staff Login</h2><?php if($err):?><p class="alert"><?=$err?></p><?php endif?><form method="post"><input type="hidden" name="csrf" value="<?=csrf()?>"><div class="field"><label>Username</label><input name="username" required></div><div class="field"><label>Password</label><input name="password" type="password" required></div><button>Sign in</button></form></div><?php footer();
